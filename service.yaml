apiVersion: v1
kind: Service
metadata:
  name: app-service
  labels:
    app: Myapp
spec:
  selector:
    app: Myapp
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
