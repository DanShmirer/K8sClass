apiVersion: v1
kind: Service
metadata:
  name: my-app-service
  labels:
    app: nginx
spec:
  selector:
    app: Myapp
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
